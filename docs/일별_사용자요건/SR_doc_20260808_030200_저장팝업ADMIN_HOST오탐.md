* 최종 버전 --> 2.0.43
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-07 17:54 (UTC) / 2026-08-08 02:54 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 03:02 (KST)
3. 반영된 프로그램 버전: 2.0.43
4. 최종본 위치: GitHub (브랜치 `cursor/fix-save-popup-admin-host-ed7e` → main 병합), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

"저장하기"버튼 누름시..."팝업"창이  떠야 하는데 전혀 뜨질 않음..
여기에 원인이 있다... 찾아봐...

6. 근본 원인 (코드 레벨 확정):
`popups(page)` 함수가 같은 관리자사이트(ADMIN_HOST=tmg1898.cafe24.com) 도메인의
새 창을 전부 제외하도록 되어 있었음(원래는 5·6항 검색팝업 판별용으로,
외부 쇼핑몰 팝업만 검색팝업으로 인정하기 위한 필터였음).

그런데 저장하기(9항) 클릭 후 뜨는 "처리중/완료" 팝업은 검색 대상 외부
사이트가 아니라 같은 관리자사이트(ADMIN_HOST) 자체 새 창으로 뜨는
경우가 많음. 저장 관련 로직(save_result_signal_present,
final_save_popup_still_open, find_mango_collect_alert,
collect_alert_baseline, dismiss_mango_alert_ui, verify_mango_collect_alert,
before_popup_ids 계산)이 전부 이 `popups()`를 그대로 재사용하고 있어서,
실제 브라우저에는 저장 팝업이 열려 있어도 코드는 "팝업이 전혀 없다"고
판단해 계속 대기하다 타임아웃/오류로 이어졌음.

7. 조치:
- ADMIN_HOST를 제외하지 않는 `save_popups()`를 신설하고,
  저장(9~11항) 관련 모든 팝업 탐지·기준집합(before_popup_ids)·정리 로직을
  `save_popups()`로 교체.
- 검색(5·6항)은 기존 `popups()`(외부사이트만) 그대로 유지 — 회귀 방지.
- `overlay_status`/다음행 진입 전 정리(`finalize_row_overlays`,
  `ensure_overlays_closed_before_next`)도 ADMIN_HOST 저장팝업을
  놓치지 않도록 `save_popups()`/`close_save_popups()`로 함께 정리.
- 회귀테스트 `test_save_popup_on_admin_host_is_detected` 추가
  (ADMIN_HOST 도메인 새 창을 Playwright route로 모킹해 검증) — 18/18 PASS.
