* 최종 버전 --> 2.1.49
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-19 23:57:00 KST (아래 원문 1번 기준, 이후 원문 2~10번 순차 요청)
2. 요구사항 반영 완료 날짜·시간: 2026-08-20 01:55:52 KST
3. 반영된 프로그램 버전: 2.1.49
4. 최종본 위치: GitHub (본 커밋 브랜치, `cursor/add-p1-102-program-e2d4`)

5. 사용자 작성 원문 전체 (총 10건, 접수 순서대로):

[원문 1] (첨부 스크린샷 1건 — 더망고 URL 입력칸 placeholder만 보이는 화면)
```
url상품검색하기 필드에 데이터가 입력이 안되는데...

그리고 sub로그, 1,2행스크린샷을 보여줘..
```

[원문 2]
```
망고와 연동하여 작업하는 화면이 전혀 안보여...
```

[원문 3]
```
너...잘되던 프로그램을 또 망가뜨린거야?
```

[원문 4]
```
-지금 안되는건 어제,오늘 잘못된 소스 수정이 원인임...

-어제와 오늘 P2 작업전 버전의 소스와 비교하여 ....
  망고와 연동하여 실행하는 영역은 절대로 수정하지 말고...원본 그대로 유지할것 ...

-요건중 사이트명 일치여부 검증관련 요건은 MUSINSA.COM 선택한건지만 체크해...
 다른 부분 소스는 수정하면 안되...
```

[원문 5]
```
중단하고...

P2요건 원문 그대로 보여줘...
```

[원문 6]
```
최종 요건을 다시 정리할게
<P2 프로그램 변경건>에 한할것..

1. 1.디렉토리 파일목록 
   폴더 상단에..."수집사이트명" 과 입력그리드
                    "수집사이트URL" 과 "입력그리드"...

2. 망고와 연동시...
    1) 위  "수집사이트명" "수집사이트URL" 입력값이
        망고에서 선택한 정보와 일치하는지 비교후 다르면 "상세한 오류 메세지"와 함께 
        프로그램 실행을 중단할것...

   2) 화면UI :카테고리 URL목록 리스트에는 "최종카테고리명"을 "상위 최종카테고리명"을 대신하여 화면에 보여줄것 

   3) 망고와 연동시 "수집필터명"을 동일하게  "최종 카테고리명"으로 반영할것..

3. 위 항에 망고연동 관련기능은 절대로 변경하지 말것...
```

[원문 7]
```
망고 로그인 과정에서 문제가 있나?
```

[원문 8] (첨부 스크린샷 1건 — 더망고 "상품데이터 대량수집" 화면 좌측 "대량수집 사이트" 목록,
"MUSINSA.com" 항목이 강조(선택)되어 표시됨)
```
수집사이트 URL 표기위치 ...
```

[원문 9] (첨부 스크린샷 1건 — 크롬 개발자도구로 위 "MUSINSA.com" 강조 행을 검사(Inspect)한
HTML/CSS 화면. `#site_list`, `.sites.active`, `.text-name`(title="MUSINSA.com"),
`a.icon-link`(href="https://www.musinsa.com") 구조가 그대로 노출됨)
```
(원문 없음 — 스크린샷만 첨부)
```

[원문 10]
```
버전관리 실행
```

6. 반영 내용 요약:

- **회귀 원인 파악(원문 1~3)**: 직전 커밋(v2.1.49 초안)에서 추가한 `verify_selected_site()`가
  더망고 화면의 실제 표시 위치를 모른 채 화면 전체 텍스트를 뒤지는 방식으로 만들어져 있어,
  오탐 시 로그인 직후 전체 프로그램이 조용히 중단되는(브라우저가 더 이상 반응하지 않는)
  회귀를 유발할 수 있었음을 확인.
- **범위 확정(원문 4~6)**: "망고와 연동하여 실행하는 영역"(로그인·URL입력·검색·저장 등)은
  절대 수정하지 않고 원본 그대로 유지. `P2/batch_steps.py`, `board/shot_viewer.py`의
  요건 외 부수적 로그 문구 변경도 전부 원복. `P2/collect.py`의 변경 범위를
  `read_excel()`(라벨 컬럼)과 `verify_selected_site()`(신규 함수) + CLI 인자 2개로만 한정.
- **정확한 선택자 확보(원문 7~9)**: 사용자가 제공한 스크린샷·개발자도구 검사 결과로,
  더망고 좌측 "대량수집 사이트" 목록의 정확한 DOM 구조를 확인:
  `#site_list .sites.active .text-name`(사이트명, title 속성) /
  `#site_list .sites.active a.icon-link`(사이트URL, href 속성).
  이 선택자로 `verify_selected_site()`를 재구현해, 요건 원문대로 **불일치 시 상세오류와
  함께 즉시 중단**하도록 복원(단, `#site_list` 자체를 못 찾는 경우만 안전하게 건너뜀).
- **최종 반영(원문 6의 1~3항)**:
  1) P2 화면 상단에 "수집사이트명"·"수집사이트URL" 입력칸 추가 (board/app.py)
  2-1) 더망고 선택 사이트와 다르면 상세오류로 즉시 중단 (P2/collect.py verify_selected_site)
  2-2) 카테고리URL목록 화면에 "최종 카테고리명" 우선 표시, 없으면 "상위 최종 카테고리명"
       (board/library.py)
  2-3) 수집필터명(검색필터명)에 "최종 카테고리명" 우선 반영, 없으면 "상위 최종 카테고리명"
       (P2/collect.py read_excel)
  3) 망고 로그인·URL입력·검색·저장 등 실행 로직은 변경하지 않음 (git diff로 확인)

7. 코드 변경 파일: `P2/collect.py`, `P2/tests/test_site_verify.py`, `board/app.py`,
   `board/library.py`, `board/tests/test_category_url_rows.py`, `README.md`, `P2/README.md`,
   `VERSION.txt`.

8. 테스트: `P2/tests/test_site_verify.py`(실제 DOM 구조를 흉내낸 가짜 Locator로 검증) 11건,
   기존 `P2/tests`(느린 브라우저 테스트 제외) 19건, `board/tests` 21건, `P1_102/tests` 25건
   — 전량 통과(총 70건). Xvfb 기반 보드 P2 탭·전체 탭 전환 스모크 테스트 통과.
