* 최종 버전 --> 2.0.60
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 05:43 (UTC) / 2026-08-08 14:43 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 14:50 (KST)
3. 반영된 프로그램 버전: 2.0.60
4. 최종본 위치: GitHub main (병합 완료), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

다음데이더 처리시 <필드 초기화가 안되는 문제 수정 조치>

2번쨰 자료,
--> 처음 2단계 시작시...

1번째 자료처럼,
 --> 로그인후 시도하는 2단계가 동일한 액션인지 확인해서 -->
 -->1번째 자료와 동일한 액션으로 2단계 작업을 수행해...

2단계 수행시,
  --> 1번째 자료와 2번쨰 자료의 동작 순서가 서로 다른 듯함

6. 원인 규명 (사용자 지적이 정확함):
- 1번째 입력행의 "2단계 초기화"는 실제로는 main()의 ensure_ready_page()가
  로그인 직후 미리 대량수집 화면으로 실제 메뉴클릭 이동을 해두고, 그
  다음 run_row_batch의 step02_init()이 호출될 땐 "이미 그 화면"이라서
  reset_to_bulk_menu() 내부의 단축경로("BULK_PATH 이고 검색버튼 보이면
  메뉴클릭 생략")가 걸려 사실상 아무 동작도 하지 않고 지나감.
- 2번째 이후 입력행은 이런 사전 준비가 없어서, step02_init()의
  reset_to_bulk_menu() 호출이 초기화의 전부였는데, 직전 행 처리 후
  화면 상태에 따라 단축경로를 타기도(아무 동작 없음) / 실제 메뉴클릭을
  하기도 해서 행마다 동작이 달라지고, 실제 메뉴클릭(서버 재요청) 없이
  넘어가는 경우 이전 행의 잔여 상태가 남아 필드 초기화가 제대로 안 되는
  현상으로 이어짐.

7. 조치 내용:
- `P2/collect.py`: `reset_to_bulk_menu()`/`_reset_to_bulk_menu_once()`에
  `force` 매개변수 추가 — force=True면 단축경로를 쓰지 않고 항상 실제
  메뉴클릭(상품데이터수집 → 대량데이터수집)을 수행.
- `P2/batch_steps.py`(`step02_init`): `ctx.input_ordinal > 1`(2번째 이후
  입력)이면 항상 `force=True`로 호출 — 1번째 입력이 사전 준비 단계에서
  거친 것과 동일한 "실제 메뉴클릭" 동작을 2번째 이후에도 매번 수행하도록
  통일. 1번째 입력은 기존처럼 단축경로(이미 도착해 있으면 생략, 속도
  요건 유지)를 그대로 사용.
