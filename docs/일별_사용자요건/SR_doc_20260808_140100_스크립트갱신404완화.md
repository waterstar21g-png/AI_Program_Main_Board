* 최종 버전 --> 2.0.57
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 05:01 (UTC) / 2026-08-08 14:01 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 14:10 (KST)
3. 반영된 프로그램 버전: 2.0.57
4. 최종본 위치: GitHub (브랜치 `cursor/meta-oneline-step9-fix-6854`), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

] No running board process
[BOOT] Refresh scripts from GitHub main...
  [WARN] update-if-newer.ps1 : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
  [WARN] boot-from-icon.ps1 : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
  [WARN] stop-board.ps1 : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
  [WARN] refresh-icons.ps1 : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
  [WARN] start.bat : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
  [WARN] run.bat : 원격 서버에서 (404) 찾을 수 없음 오류를 반환했습니다.
[BOOT] Local VERSION (before update) = 2.0.55
[BOOT] Running update-if-newer.ps1 ...
[OK] updater self-refreshed from GitHub - re-exec

욕나오게 하지 마라

6. 원인 규명:
- GitHub 저장소(raw.githubusercontent.com)에서 6개 파일 모두 정상 200 OK로
  확인됨(서버측 문제 아님). 즉 이 404는 사용자 PC/네트워크 환경에서만
  발생 — 구형 PowerShell 5.1의 TLS 기본값(1.0)이 GitHub 요구사항(TLS1.2+)과
  안 맞거나, 사내망/백신의 SSL 검사가 raw.githubusercontent.com 요청을
  가로채 가짜 404를 돌려주는 경우로 추정됨.
- 기능적으로 치명적이진 않음(로컬 캐시 파일로 계속 진행되어 결국 보드는
  정상 기동됨)이나, 매번 붉은 경고 6줄이 떠서 불쾌감을 유발함.

7. 조치 내용:
- `boot-from-icon.ps1`, `update-if-newer.ps1`, `run.bat`: 스크립트 시작 시
  TLS 1.2/1.1/1.0을 명시적으로 활성화(`ServicePointManager.SecurityProtocol`).
- 각 파일 다운로드를 raw.githubusercontent.com 2회 재시도 후 jsdelivr
  CDN 미러로 최종 폴백하도록 변경(다른 경로이므로 특정 도메인 차단 시 우회).
- 경고 출력을 파일당 1줄(6줄)에서 성공/실패 건수 요약 1줄로 축소(실패
  목록은 이어서 표시), 상세 오류는 어두운 색상으로 별도 줄에 표시.
