* 최종 버전 --> 2.1.54
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-20 12:08:00 KST
2. 요구사항 반영 완료 날짜·시간: 2026-08-20 12:19:22 KST
3. 반영된 프로그램 버전: 2.1.54
4. 최종본 위치: GitHub (브랜치 `cursor/final-category-label-and-save-count-5191`, PR 생성)

5. 사용자 작성 원문 전체 (총 1건):

[원문 1]
```
현재 프로그램에서 하나만...다음 3가지만 변경해 

1. 카테고리URL목록 : "상위 최종 카테고리명" -> "최종 카테고리명" 으로 변경

2. <망고내>...."검색필터명" : "상위 최종 카테고리명" -> "최종 카테고리명" 으로 변경

3. 저장상품수 : "3개" -> "50개"
```

---

## 반영 내용

### 1. 카테고리URL목록 표시명

- `board/library.py` `read_category_url_rows()` — `최종 카테고리명` 열을 우선
  사용하고, 없으면 `상위 최종 카테고리명` 으로 대체(옛 엑셀 하위 호환).
- `board/tests/test_category_url_rows.py` — 우선순위·폴백 테스트 2건 추가.

### 2. 망고 "검색필터명"

- `P2/collect.py` `read_excel()` — 같은 우선순위 규칙 적용. 검색필터명에
  들어가는 값이 `최종 카테고리명` 이 된다.
- 실행로그·HTML 갤러리 메타·스크린샷 캡션 표기를 `최종 카테고리명=` 으로 정정
  (`P2/collect.py` 6곳 + 입력목록 헤더 1곳, `P2/batch_steps.py` 2곳,
  `board/shot_viewer.py` 1곳).
- **연계 수정**: `P3_필터_갱신/update_filters.py` `FILTER_HEADERS` 순서에서
  `최종 카테고리명` 을 `상위 최종 카테고리명` 앞으로 옮김. P3 는 엑셀 필터명과
  망고 행 필터명을 비교해 매칭하므로, P2 가 저장한 필터명과 기준이 어긋나면
  전 행이 불일치로 넘어간다. `board/app.py` 의 P3 미리보기 목록도 동일 규칙.
- `P2/tests/test_filter_name_column.py` 신규 — 우선순위·폴백·오류 3건.

### 3. 저장상품수 3 → 50

- `P2/collect.py` `DEFAULT_SAVE_COUNT = 3` → `50`.
- `board/app.py` P2 실행 인자 `"3"` → `"50"`.
- `fill_save_modal_fields()` — 저장상품수 칸에 값을 **직접 입력**하도록 변경.
  최대 4라운드 확인·재입력하고, 그 과정에서 검색필터명이 흔들리면 확정 실패.
- `P2/tests/test_save_popup_gate.py` — 모달 입력 테스트 4건을 새 동작에 맞게
  재작성(필드 구분 / 기본값 3 덮어쓰기 / 이미 맞으면 재입력 없음 / 빈 칸 채우기).

## 요건 충돌 처리 (중요)

2026-08-08 요건(SR_doc_20260808_120900) 은 **"저장상품수 입력필드값은 변경하지
마 — 원래 세팅값 그대로 둬"** 였고, 그 이후 코드는 이 칸을 읽기만 했다.
망고 상품저장설정 모달의 기본값이 **3** 이므로, 그 요건을 유지한 채로는 상수만
50 으로 바꿔도 실제 저장은 계속 3개가 된다.

이번 요건("저장상품수 3개 → 50개")이 더 최신이고 값 자체를 지정하고 있으므로,
2026-08-08 요건을 **대체**하는 것으로 처리하고 모달에 50 을 직접 입력하도록
바꿨다. 두 필드가 같은 엘리먼트로 잡히는지 확인하는 기존 안전장치는 유지했다.

※ 다시 "원래 세팅값 유지"로 돌리려면 `fill_save_modal_fields()` 의 저장상품수
입력 루프만 제거하면 된다.
