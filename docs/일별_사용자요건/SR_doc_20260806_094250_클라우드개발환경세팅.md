# SR — Cloud Agent 개발환경 세팅

* 최종 버전 --> 1.2.2
* 로컬PC 보관 --> Not-OK (Cloud Agent VM 세팅 작업)
* Vercel 배포 --> Not-OK (배포 대상 아님)
* GitHUB Commit --> OK

## 메타데이터

| 항목 | 값 |
|------|-----|
| **요구사항 요청** | 2026-08-06 09:32 (KST) |
| **요구사항 반영 완료** | 2026-08-06 09:43 (KST) |
| **반영된 프로그램 버전** | 1.2.2 |
| **최종본 위치** | GitHub (`.cursor/environment.json`) |

## 반영 내용 요약

- `.cursor/environment.json` 신규 생성 (Cursor Cloud Agent 개발환경 정의).
  - `install`: `npm ci` (의존성 설치, 멱등)
  - `terminals`: `npm run dev` (Next.js 개발 서버, http://localhost:3000)
  - `ports`: 3000 노출
- 기본 베이스 이미지의 Node 22 사용 (별도 Dockerfile/스냅샷 불필요).
- 검증: `npm ci`(멱등) → `tsc --noEmit` → `next build` → 개발 서버 기동 → `/api/analyze` 키워드 조회(end-to-end) → 브라우저 UI 조회(데모 데이터 가격비교) 확인.

## 사용자 작성 원문

```
/env-setup Set up a new development environment for this codebase. Follow the “Set up a new environment” workflow in the env-setup skill. Run the relevant applications and demonstrate that the environment works end to end.
```
