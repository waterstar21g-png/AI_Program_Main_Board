* 최종 버전 --> 2.0.61
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 05:47 (UTC) / 2026-08-08 14:47 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 14:52 (KST)
3. 반영된 프로그램 버전: 2.0.61
4. 최종본 위치: GitHub main (병합 완료), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

PowerShell 스크립트를 거치지 않고 브라우저로 직접 받으면 됩니다.

브라우저에서 https://github.com/waterstar21g-png/AI_Program_Main_Board 접속
초록색 Code 버튼 클릭 → Download ZIP 클릭
다운로드된 zip 파일 압축 풀기
기존 D:\My_Project\AI_Program_Main_Board 폴더 안 내용물을 방금 푼 파일들로 전부 덮어쓰기
run.bat 더블클릭해서 실행

bat 로 만들어

6. 조치 내용:
- `update-by-zip.bat` 신규 생성 — git pull이 계속 실패하는 PC를 위한
  대안. codeload.github.com에서 최신 main 브랜치를 ZIP으로 통째로
  받아(raw.githubusercontent.com/git와 다른 경로이므로 우회 가능성 있음)
  임시폴더에 풀고, 현재 프로젝트 폴더에 덮어쓴 뒤 자동으로
  `run.bat --noupdate`로 보드를 시작함. 사용자가 브라우저로 수동으로
  하던 작업을 더블클릭 한 번으로 자동화.
  - 로그·크롬프로필 등 로컬 전용 파일(.gitignore 대상)은 ZIP에 없으므로
    덮어쓰기 과정에서 보존됨.
  - 다운로드 실패 시 브라우저 수동 다운로드 안내를 그대로 출력.
- README.md: 이 대안을 트러블슈팅 섹션에 안내 추가, v2.0.58에서 제거된
  "부트 스크립트 개별 재다운로드" 관련 옛 설명 정리.
- (부가 발견·수정) `run.bat`에 한글(P1/P2 설명줄)이 있는데 `chcp` 설정이
  없어 콘솔에서 한글이 깨져 보이는(mojibake) 문제 확인 — 사용자 스크린샷
  최초 화면에서도 확인됨. `run.bat`/`update-by-zip.bat` 모두 맨 위에
  `chcp 65001 >nul` 추가로 수정.
