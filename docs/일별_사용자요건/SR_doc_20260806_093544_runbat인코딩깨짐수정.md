* 최종 버전 --> 2.2.9
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST) — 사용자 로컬 `.\run.bat` 실행 오류 보고

2. 요구사항 반영 완료 날짜·시간
2026-08-06 09:35:44 (KST)

3. 반영된 프로그램 버전
2.2.9

4. 최종본 위치
- 로컬PC: run.bat → boot.ps1 → run.ps1 동기화 후 localhost:3000 (APP_VERSION 2.2.9 확인)
- Vercel: Not-OK
- GitHub: branch cursor/fix-runbat-encoding-dcbc (merge to main 후 사용)

5. 사용자 작성 원문 전체

```
PS C:\Users\water\AI_Program_Main_Board> .\run.bat
'cp'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
'tlocal'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
'/d'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
'cho'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
'wershell'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
지정된 경로를 찾을 수 없습니다.
지정된 경로를 찾을 수 없습니다.
't'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
지정된 경로를 찾을 수 없습니다.
[FATAL] Could not download run.ps1 v2.2.8
Paste this in PowerShell:
irm https://api.github.com/repos/waterstar21g-png/sangpum-capture-price/contents/run.ps1?ref=main -Headers @{Accept='application/vnd.github.raw';'User-Agent'='x'} -OutFile run.ps1; .\run.bat
계속하려면 아무 키나 누르십시오 . . .
```

6. 반영 내용
- run.bat를 ASCII/CRLF 최소 부트로 교체 (boot.ps1 호출만)
- boot.ps1 추가: run.ps1을 UTF-8(무BOM)로 다운로드 후 실행
- run.ps1 Download-RepoFile: UTF-16/BOM 제거, .bat는 CRLF로 저장
- 복구 안내에서 irm -OutFile(UTF-16 위험) 제거 → WriteAllText UTF-8 무BOM 사용
- APP_VERSION 2.2.9
