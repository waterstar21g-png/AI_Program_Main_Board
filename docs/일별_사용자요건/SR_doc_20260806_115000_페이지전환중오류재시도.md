* 최종 버전 --> python-collector v1.3.1
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK (해당 없음)
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
python-collector v1.3.1

4. 최종본 위치
- 로컬PC: python-collector/collect.py
- GitHub: main

5. 사용자 작성 원문 전체

```
품데이터수집 -> 대량데이터수집 클릭
Traceback (most recent call last):
  File "...collect.py", line 649, in reset_to_bulk_menu
    if href.count() > 0:
playwright._impl._errors.Error: Locator.count: Execution context was destroyed, most likely because of a navigation
```

6. 반영 내용
- 원인: 로그인 직후 사이트가 아직 리다이렉트/네비게이션 중인 상태에서
  reset_to_bulk_menu가 href.count() 등으로 DOM을 조회하면 실행 컨텍스트가
  파괴되어 오류가 남 (safe_goto로 이미 고친 page.goto()와 같은 계열의
  문제가 다른 DOM 조회 지점에서도 발생)
- with_nav_retry() 공통 재시도 헬퍼 추가: "Execution context was
  destroyed / Target closed" 계열 오류가 나면 페이지 로딩 안정을
  기다린 뒤 최대 3회 재시도, 무관한 오류는 그대로 전파
- reset_to_bulk_menu, wait_bulk_ready, url_input을 각각
  _once 버전으로 분리하고 with_nav_retry로 감쌈
- 검증: with_nav_retry가 (a) 지정 횟수 내 성공 시 정상 반환
  (b) 재시도 소진 시 원래 예외 그대로 전달 (c) 무관한 예외는 재시도 없이
  즉시 전파 — 3가지 모두 단위 검증. 전체 0→1→2→3→4 흐름 재검증(회귀 없음)
