* 최종 버전 --> python-collector v1.0.0 (기존 웹앱 2.3.1은 유지)
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK (해당 없음 — 로컬 전용 파이썬 스크립트)
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
python-collector v1.0.0 (신규, 독립 실행)

4. 최종본 위치
- 로컬PC: python-collector/collect.py (pip install 후 바로 실행)
- Vercel: 해당 없음
- GitHub: main, /python-collector 디렉터리

5. 사용자 작성 원문 전체

```
야 염병아 ...

다 걷어 치우고.

이 요건으로 새로 개발해봐

0. 초기화 : 상품데이터수집 -> 대량데이터수집 클릭

1. URL상품검색하기 --> <필드값 입력후> --> <팝업창이> 없어질때까지 대기

2. 검색된 상품 모두저장 <클릭>후 -> <팝업창>에서 <검색필터명> 입력후 -> 
   <저장하기> 버튼 클릭

3. 팝업창이 없어질때까지 대기

4. -> 0. 초기화

(후속 메시지) 파이썬으로 간단하게 작성해....
```

6. 반영 내용
- Next.js/npm/run.bat/Turbopack 전부 배제. python-collector/collect.py 단일 스크립트로 신규 작성
- 요건 0~4를 함수 1:1로 매핑 (reset_to_bulk_menu / step1 검색 / step2 모두저장·필터명·저장 / step3 팝업대기 / 4→0 루프)
- 팝업은 절대 열거나 닫지 않음 — 항상 "스스로 닫힐 때까지"만 대기
- 엑셀(상위 최종 카테고리명, 최종 카테고리 URL주소) 그대로 파싱
- 로그인은 최초 1회 수동, 이후 .chrome-profile 프로필로 유지

7. 자체 검증 (실제 더망고 사이트 접근 불가한 환경이라 mock HTML로 대체 검증)
- 로컬 mock 페이지(팝업 자동 열림/자동 닫힘, 저장 모달 자동 열림/닫힘)로 전체 0→1→2→3→4 사이클을 3행 연속 실행 → 전부 통과
- 저장 로그 검증: 각 행의 검색필터명이 올바르게 입력되어 저장됨 확인
- 버그 발견 및 수정: Playwright Python 동기 API는 time.sleep() 중 팝업 열림/닫힘 이벤트를 처리하지 못함 → page.wait_for_timeout()으로 전면 교체 (이 문제를 안 고쳤으면 실사이트에서도 팝업이 계속 "안 닫힌 것처럼" 보였을 것)
- 팝업이 없는데도 매 행 10초씩 헛대기하던 문제 발견·수정 (grace_sec 도입)
