* 최종 버전 --> 2.0.67
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 07:40 (UTC) / 2026-08-08 16:40 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 16:43 (KST)
3. 반영된 프로그램 버전: 2.0.67
4. 최종본 위치: GitHub (브랜치 `cursor/fix-verify-maxrows2-ac92`), 로컬PC는 머지 후 아이콘 클릭, Vercel 해당 없음

5. 사용자 작성 원문 전체:

2행만 작업하고 중단하는 이유가 뭐야...도대체?

이게 몇번째9 요구사항 전달이야?

개 자식아....

6. 원인 규명 (진짜 원인 — 이전 수정들이 못 잡은 지점):
- 보드 체크박스 "1·2행 전과정 스크린샷" 기본값 ON
  → 실행 시 `collect.py`에 `--verify` 전달
  → `collect.py`가 verify면 `max_rows = 2` 로 강제
  → 엑셀이 100행이어도 **2행만 처리하고 정상 종료(exit 0)**
  → 화면에 에러 없이 "끝난 것처럼" 보임 = "2행에서 중단"
- v2.0.53에서 보드의 `--max-rows 2` 만 제거했고,
  collect.py 안의 verify→max_rows=2 게이트는 그대로 남겨둔 것이
  반복 재발 원인. (예외/팝업 쪽 수정 v2.0.60~62는 별개 경로)

7. 조치 내용:
- `P2/collect.py`: verify 시 max_rows=2 강제 코드 삭제.
  `--verify`는 스크린샷·무중단 의미만. 행 수 제한은 `--max-rows`만.
- 회귀 테스트 `P2/tests/test_row_loop_continues.py` 포함 (8 passed).
- 루프 밖 예외도 실행로그(ctx.info)에 남겨 조용한 종료 방지.
