* 최종 버전 --> 2.0.68
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 07:51 (UTC) / 2026-08-08 16:51 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 16:54 (KST)
3. 반영된 프로그램 버전: 2.0.68
4. 최종본 위치: GitHub (브랜치 `cursor/category-url-list-ui-ac92`), 로컬PC는 머지 후 아이콘 클릭, Vercel 해당 없음

5. 사용자 작성 원문 전체:

1. 리렉토리에 있는 화일 목록을 보여줄것 --> 리스트 박스로 "스크롤 기능

2. 보관목록 -> "카테고리URL목록" 으로 이름을 변경하고
 --> "리스트 박스"로 만들어 "엑셀 목록" 전체를 리스트로 보여 줄것 ..
 --> "현재 작업 진행 진행중"인 행을 적색 으로 표시 할것

(스크린샷1: 디렉터리 .xlsx 파일명이 리스트박스+스크롤로 표시된 UI)
(스크린샷2: "2. 보관 목록 (재실행 시 여기서만 선택)" 섹션 —
 파일명 1개가 파란 선택으로 표시된 UI)

6. 조치 내용:
- `board/app.py`:
  1) 섹션1 제목 "디렉터리 파일 목록" — Listbox + 세로 스크롤 + 마우스휠.
     폴더의 .xlsx 파일명을 표시. 선택/더블클릭 시 엑셀 로드.
  2) 섹션2 제목 "보관 목록" → **"카테고리URL목록"** 으로 변경.
     선택한 엑셀의 전체 행(순번|카테고리명|URL)을 Listbox+스크롤로 표시.
     수집 중 META 순번 갱신 시 해당 행을 **적색(▶ + 빨간 글자/배경)** 표시.
- `board/library.py`: `read_category_url_rows()` 추가.
- 단위테스트 `board/tests/test_category_url_rows.py` 통과.
