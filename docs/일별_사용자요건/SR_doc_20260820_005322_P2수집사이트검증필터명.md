* 최종 버전 --> 2.1.49
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-19 23:27:00 KST (아래 원문 1번 기준, 이후 원문 2~5번 순차 요청)
2. 요구사항 반영 완료 날짜·시간: 2026-08-20 00:53:22 KST
3. 반영된 프로그램 버전: 2.1.49
4. 최종본 위치: GitHub (본 커밋 브랜치, `cursor/add-p1-102-program-e2d4`)

5. 사용자 작성 원문 전체 (총 5건, 접수 순서대로):

[원문 1]
```
P2 프로그램을 수정해줘....

1. 1.디렉토리 파일목록 
   폴더 상단에..."수집사이트명" 과 입력그리드
                    "수집사이트URL" 과 "입력그리드"...

2. 망고와 연동시...
    1) 위  "수집사이트명" "수집사이트URL" 입력값이
        망고에서 선택한 정보와 일치하는지 비교후 다르면 "상세한 오류 메세지"와 함께 
        프로그램 실행을 중단할것...

   2) 화면UI :카테고리 URL목록 리스트에는 "최종카테고리명"을 "상위 최종카테고리명"을 대신하여 화면에 보여줄것 

   3) 망고와 연동시 "수집필터명"을 동일하게  "최종카테고리명"을 반영할것..
```

[원문 2]
```
저장상품수 -> 100개로 수정
```

[원문 3]
```
버전관리 실행...
```
(위 원문2 반영 도중 접수 — 실제 커밋은 이후 원문4·5까지 반영한 뒤 이번 건으로 처리)

[원문 4]
```
저장상품수 _> 3개로 유지
```
(원문2에서 100개로 바꾼 것을 다시 3개로 되돌림 — 최종적으로 저장상품수는 기존값 3 유지)

[원문 5]
```
커밋.배포해...
```

6. 반영 내용 요약:

- **P2 화면**: "1. 디렉터리 파일 목록" 상단에 "수집사이트명"·"수집사이트URL" 입력칸 추가.
- **수집사이트 검증**: `P2/collect.py`에 `verify_selected_site()` 추가 — 대량수집 화면
  준비 완료 후(행 처리 시작 전) 입력값이 더망고 화면 정보와 다르면 상세 오류 메세지
  (입력값·화면 URL·화면 제목 포함)와 함께 즉시 중단. 보드가 `--site-name`/`--site-url`
  로 자동 전달, 비워두면 검증 생략(하위 호환).
  - ★참고: 더망고는 로그인 필요한 비공개 화면이라 정확한 표시 위치를 확인하지 못해,
    화면 전체 텍스트·제목·현재 URL에서 값을 찾는 방식으로 구현 — 실제 동작이 다르면
    추가 조정 필요.
- **카테고리URL목록 화면 표시**: `board/library.py`의 `read_category_url_rows()`가
  "상위 최종 카테고리명" 대신 "최종 카테고리명" 열을 읽어 표시.
- **수집필터명(검색필터명)**: `P2/collect.py`의 `read_excel()`도 동일하게 "최종
  카테고리명" 열을 읽어 더망고 검색필터명 입력에 반영. 관련 로그 문구도 함께 갱신
  (`P2/batch_steps.py`, `board/shot_viewer.py` 포함).
- **저장상품수**: 100개로 변경 요청 후 다시 3개로 되돌림 — 최종적으로 기존값
  `DEFAULT_SAVE_COUNT = 3` 그대로 유지(변경 없음).

7. 코드 변경 파일: `P2/collect.py`, `P2/batch_steps.py`, `P2/README.md`,
   `P2/tests/test_site_verify.py`(신규), `board/app.py`, `board/library.py`,
   `board/shot_viewer.py`, `board/tests/test_category_url_rows.py`, `README.md`,
   `VERSION.txt`.

8. 테스트: `P2/tests/test_site_verify.py` 신규 7건, 기존 `P2/tests`(느린 브라우저
   테스트 제외) 19건, `board/tests` 21건, `P1_102/tests` 25건 전량 통과. Xvfb 기반
   보드 P2 탭 신규 입력칸·전체 탭 전환 스모크 테스트 통과.
