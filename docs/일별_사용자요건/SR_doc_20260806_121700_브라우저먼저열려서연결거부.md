* 최종 버전 --> 2.4.2
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
2.4.2

4. 최종본 위치
- 로컬PC: .\run.ps1 -Sync 후 자동으로 브라우저가 열림(서버 준비된 뒤)
- Vercel: Not-OK
- GitHub: main

5. 사용자 작성 원문 전체

```
(스크린샷) 사이트에 연결할 수 없음
localhost에서 연결을 거부했습니다.
ERR_CONNECTION_REFUSED
```

6. 반영 내용
- 원인: run.ps1이 `Start-Process "http://localhost:3000"`으로 브라우저를
  먼저 연 다음 `npm run dev`를 실행했음 → dev 서버가 실제로 포트를 열기
  전에 브라우저가 먼저 접속을 시도해 "연결 거부"가 뜸
- 수정: npm run dev를 백그라운드 프로세스로 띄우고, 포트 3000이 실제로
  응답할 때까지(최대 60초, 0.5초 간격 폴링) 기다린 뒤에만 브라우저를 염.
  dev 서버가 중간에 죽으면 그 사실도 바로 표시
- 부수 정리: 더 이상 쓰지 않는 `npx playwright install chromium` 설치
  단계 제거 (browser-session.ts가 channel:'chrome'/'msedge'로 PC에
  이미 있는 브라우저를 쓰므로 불필요 — 시작 시간도 단축됨)
