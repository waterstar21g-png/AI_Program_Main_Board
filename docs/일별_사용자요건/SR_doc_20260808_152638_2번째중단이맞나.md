* 최종 버전 --> 2.0.65
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 15:25 (KST) / 2026-08-08 06:25 (UTC)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 15:26 (KST)
3. 반영된 프로그램 버전: 2.0.65
4. 최종본 위치: GitHub (브랜치 `cursor/fix-row2-batch-stop-9027`), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

2번째 중단시키는게 맞아?

6. 답변·조치 내용:

## 답: 아니다. 2번째에서 멈추면 안 된다.

엑셀에 자료가 남아 있는 동안에는 계속 처리해야 한다(사용자 반복 지시).
그런데 **의도적으로 2행만 처리하고 정상 종료시키는 코드**가 남아 있었다.

## 진짜 원인 (예외가 아니라 "행 수 제한")

- 보드 체크박스 **"1·2행 전과정 스크린샷"의 기본값이 ON**
  (`board/app.py::var_verify = tk.BooleanVar(value=True)`)
  → 실행 시 `collect.py` 에 `--verify` 를 넘긴다.
  보드 쪽 주석은 "행 수 제한 없음 — 엑셀 전체 처리"라고 적혀 있다.
- 그러나 `collect.py::main()` 은 그 플래그를 **행 수 제한**으로 해석했다:

      if verify and max_rows is None:
          max_rows = max(2, shot_first)   # = 2
      ...
      rows = all_rows[:max_rows]          # 3행 이후는 아예 목록에서 잘림

  → 엑셀에 자료가 100건이어도 **1·2행만 처리하고 "완료"로 끝났다.**
  에러도 안 나고 종료코드도 0이라 "2번째에서 중단"으로 보였다.

## 조치

- `--verify` 의 의미를 **스크린샷 범위·무중단**으로만 한정. 행 수는 건드리지
  않는다(엑셀 전체 처리). 행 수 제한은 `--max-rows` 로만 건다.
- `--verify` / `--max-rows` 도움말·모듈 사용법 주석을 실제 동작에 맞게 수정.
- 회귀 테스트 2건 추가 (`P2/tests/test_row_loop_continues.py`)
  - `--verify` 만 줬을 때 3행 전부 처리하는지
  - `--max-rows 2` 는 여전히 2행으로 제한하는지

  수정 이전 코드(v2.0.63)에 걸면 첫 번째가 실제로 FAIL — 이번 증상을
  그대로 재현하는 테스트임을 확인했다.
