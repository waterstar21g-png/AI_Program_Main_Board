* 최종 버전 --> 2.0.58
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 05:14 (UTC) / 2026-08-08 14:14 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 14:20 (KST)
3. 반영된 프로그램 버전: 2.0.58
4. 최종본 위치: GitHub (브랜치 `cursor/meta-oneline-step9-fix-6854`), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

(1)
작업이 엑셀 2번째자료, 5단계 중간에 중단됐는데 원인이 뭐야?
(수동으로 하면 문제 없는데)

그리고, 새로운 요건으로
지금부터는 -->  각 엑셀자료 시도는 1번으로 끝내 ..두번 시도하지마...

(2)
위 오류는 "바탕화면 아이콘 변경"작업후 발생하는 오류 잖아...

제대로 원인을 파악해.봐..

네가 저지른 실수인데 그걸 모루나?

6. 원인 규명 (정정):
- 이전 답변(v2.0.57, TLS/재시도/미러 추가)은 증상 완화였을 뿐, 진짜 원인이
  아니었음. 실제 원인은 v2.0.54("실행아이콘 클릭시 stop+update+보드재시작
  일괄처리")에서 boot-from-icon.ps1에 새로 추가한 "6개 파일을
  raw.githubusercontent.com에서 개별 다운로드" 단계 그 자체임.
  - 이 단계는 바로 다음에 실행되는 update-if-newer.ps1의 git pull이 이미
    모든 파일(자기 자신 포함)을 갱신하므로 완전히 중복된 동작이었고,
    다운로드 시도를 6배로 늘려 사용자 PC의 네트워크 이슈를 훨씬 더 자주
    노출시킴 — v2.0.54 이전에는 이런 6줄짜리 경고가 없었음(사용자 지적이
    정확함).
  - 5단계 중단(검색 팝업 6항 미확인) 원인: 상품 수가 많은 카테고리는
    더망고 확장프로그램이 팝업창 내용을 임시메모리에 적재(스크래핑)하는
    시간이 40초를 넘는 경우가 있음. 자동화는 40초에서 포기하지만, 사람이
    수동으로 하면 자연스럽게 더 기다리므로 문제가 없었던 것.

7. 조치 내용:
- `boot-from-icon.ps1`: 6개 파일 개별 raw 다운로드 단계를 완전히 삭제.
  이제 stop-board → update-if-newer.ps1(git pull) → run.bat --noupdate
  3단계만 수행 — git pull이 모든 스크립트를 이미 갱신하므로 중복이자
  오류 원인이었던 단계를 제거함.
- `P2/collect.py`: 검색 팝업 닫힘 대기(6항) 40초 → 90초로 확대, 확인/닫기
  보조 클릭을 1회성이 아니라 팝업이 남아있는 동안 8초 간격으로 계속 재시도.
- `P2/collect.py`(DEFAULT_ROW_RETRIES) · `board/app.py`(--retries): 2 → 1
  로 변경 — 엑셀 각 행은 이제 실패 시 같은 행을 다시 시도하지 않고 즉시
  다음 입력 데이터로 진행.
