* 최종 버전 --> 2.1.55
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-21 12:33 KST
2. 요구사항 반영 완료 날짜·시간: 2026-08-21 12:51 KST
3. 반영된 프로그램 버전: 2.1.55 (개발환경 설정 추가 — 코드 로직 변경 없음)
4. 최종본 위치: GitHub (브랜치 `cursor/setup-dev-environment-fd1a`, PR 생성)

5. 사용자 작성 원문 전체:

[원문]
```
/env-setup Set up a new development environment for this codebase. Follow the "Set up a new environment" workflow in the env-setup skill. Run the relevant applications and demonstrate that the environment works end to end.
```

---

## 반영 내용

- `.cursor/environment.json` 신규 — Cloud Agent 환경(저장소 관리형).
  - `install`: `.cursor/install.sh` 실행
  - `terminals`: `Xvfb :99` 가상 디스플레이 상시 기동
- `.cursor/install.sh` 신규 (idempotent) — 시스템 패키지(python3-tk, xvfb,
  imagemagick, xdotool, fonts-nanum, fonts-noto-cjk), Python 의존성
  (requirements.txt + pytest), Playwright(Chromium + 브라우저 의존성) 자동 설치.
- `.gitignore` 에 `.venv/` 추가.
- `VERSION.txt` / `README.md` 첫 줄 버전 2.1.54 → 2.1.55.

## 검증 (end-to-end)

- 전체 테스트 117건 통과. P2 타이밍 테스트 1건은 **환경과 무관한 기존 결함**
  (in-page 결과 레이어를 완료신호 탐지가 못 봐서 180초 대기 후 실패) — 개발환경
  설정 범위 밖이라 손대지 않음.
- Playwright Chromium 정상 기동 확인.
- Tkinter 보드를 Xvfb(:99)에서 헤드리스로 실행 → 6개 프로그램 탭(P1·P1_102·
  P1_101·P1_ZARA_DE·P2·P3_필터_갱신) 모두 렌더링·전환 확인(한글 폰트 정상).
