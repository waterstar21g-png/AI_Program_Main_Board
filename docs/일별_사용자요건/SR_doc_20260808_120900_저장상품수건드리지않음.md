* 최종 버전 --> 2.0.49
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-07 20:05 (UTC) / 2026-08-08 05:05 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 12:09 (KST)
3. 반영된 프로그램 버전: 2.0.49
4. 최종본 위치: GitHub (브랜치 `cursor/keep-save-count-default-ed7e` → main 병합), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

자...프로그램 다시 수정해...

1. "검색필터명" 입력필드값에만 --> 엑셀 데이터를 입력하고,,,

2." 저장상품수" 입력필드값은 --> 변경하지마  --> 즉, 원래 세팅값 그대로 둬...

OK?

6. 조치 내용:
- `fill_save_modal_fields()`를 재작성 — 검색필터명 칸에만 엑셀 데이터
  (상위 최종 카테고리명)를 입력. 저장상품수 칸은 어떤 경우에도 쓰기
  동작(type_into)을 하지 않음.
- 저장상품수 칸의 "원래 세팅값"을 미리 읽어두고, 함수가 끝날 때 그
  값이 그대로인지만 확인(변경 여부 경고 로그만, 우리가 되돌리지 않음).
- 함수 반환값으로 이 원래 세팅값(정수)을 돌려주고, 이후 9~12항(저장
  하기·팝업·건수확인)은 이 값을 기대 저장건수로 사용하도록
  `batch_steps.run_row_batch`를 수정(기존에는 CLI --save-count 값을
  그대로 기대값으로 썼음). 저장상품수 칸이 비어있어 파싱 불가하면
  --save-count 값을 대신 사용.
- 회귀테스트 재작성: 필터만 바뀌고 저장상품수는 원래값(예: 5, 7)
  그대로인지, 이미 필터가 맞으면 재입력 로그가 없는지, 저장상품수 칸이
  비어있을 때 --save-count로 대체되는지 확인 — 25/25 PASS.
