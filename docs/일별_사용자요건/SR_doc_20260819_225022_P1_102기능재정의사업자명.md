* 최종 버전 --> 2.1.47
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-19 22:33:00 KST (아래 원문 1번 기준, 이후 원문 2~5번 순차 요청)
2. 요구사항 반영 완료 날짜·시간: 2026-08-19 22:50:22 KST
3. 반영된 프로그램 버전: 2.1.47
4. 최종본 위치: GitHub (본 커밋 브랜치, `cursor/add-p1-102-program-e2d4`) — 사용자 요청에 따라
   "버전관리 실행" 지시 전까지 로컬에서만 갱신 후, 이번 커밋에 일괄 반영.

5. 사용자 작성 원문 전체 (총 6건, 접수 순서대로):

[원문 1]
```
P1_102 프로그램 기능.

1. 기능 : 수집사이트 추출  -P1과 유사하나 일부가 다름

2. 입력으로 주어지는 정보
    - 홈페이지 주소 
    - 상위 카테고리명 1,2,.....
    - 중위 카테고리명 : 상위 카테고리명 1의 하위 목록  1-1, 1-2,.....
                              상위 카테고리명 2의 하위 목록  2-1, 2-2,.....  

3. 위 2의 정보를 바탕으로,
    1) 하위 카테고리를 추출하되, 최종카테고리명 : "하위 카테고리명" 으로 함
    2) 결과물은 P1과 동일한 OUTPUT

.자..작업을 실행해서 결과를 보여줘..빨히..
```

[원문 2]
```
P1위 INPUT중 상위 카테고리는 --> 아래처럼 4개 행으로 만들어
상위 카테고리  : <입력그리드> 1개  
중위 카테고리 :  <입력그리드> 20개

상위 카테고리  : <입력그리드> 1개  
중위 카테고리 :  <입력그리드> 20개
```

[원문 3]
```
INPUT으로 입력된 값은 프로그램 종료후 재실행시도 초기값으로 그대로 보여줘...
```

[원문 4]
```
버전관리 실행....명령전 까지는 로컬에서만 소스를 갱신하고 관리해 ..

시간절약을 위해......
```

[원문 5]
```
1. 입력항목명에 추가 : 사이트명 옆에 "사업자명" 와 그리드 추가해.

2. 최종카테고리명 재정의 : 사업자명 + '-' + 사이트명 + 상위카테고리명 + '-' + 중위 카테고리명 + '-' + 하위 카테고리명
```

[원문 6]
```
버전관리 실행
```

6. 반영 내용 요약:

- **P1_102 신규 기능 확정(원문 1)**: `P1_102/`는 P1(A-RT/ABC마트) 크롤링 로직을 기반으로 하되,
  GNB 하위(depth3) 카테고리 아래에 더 깊은 depth4 메뉴가 있어도 그 안으로 내려가지 않고
  하위 카테고리명 자체를 최종 카테고리명으로 고정(`parse_art_gnb_low_as_final`). 결과물
  엑셀 헤더·형식은 P1과 동일.
- **입력 UI 4행 구성(원문 2)**: 상위 카테고리(입력칸 1개) + 중위 카테고리(입력그리드 20개)를
  한 그룹으로 하여 2개 그룹(총 4행)을 보드에 배치. `TOP_GROUP_COUNT=2`, `MID_PER_TOP=20`.
- **입력값 재실행 복원(원문 3)**: 사이트명·URL·저장폴더·상위/중위 카테고리명(이후 사업자명 포함)을
  `P1_102/.last_input.json`에 입력 즉시 저장, 보드 재기동 시 그대로 초기값으로 표시
  (`load_last_input`/`save_last_input`, 각 입력 위젯에 `trace_add("write", ...)` 연결).
- **버전관리 보류 지시(원문 4)**: 이후 "버전관리 실행" 명령 전까지 커밋·푸시 없이 로컬 파일만 갱신.
- **사업자명 입력·최종카테고리명 재정의(원문 5)**: 사이트명 옆(같은 행)에 "사업자명" 입력칸 추가.
  "최종 카테고리명" 컬럼을
  `사업자명 + '-' + 사이트명 + 상위카테고리명 + '-' + 중위카테고리명 + '-' + 하위카테고리명`
  (원문 그대로 — 사이트명과 상위카테고리명 사이에는 구분자 없음)으로 재정의
  (`build_final_category_name`). "하위 카테고리명" 컬럼에는 실제 추출된 하위 카테고리명을 채움.
- **버전관리 실행(원문 6)**: 위 변경사항을 모아 커밋·푸시, PR 갱신.

7. 코드 변경 파일: `P1_102/crawl.py`, `P1_102/tests/test_top_alias.py`, `P1_102/__init__.py`,
   `board/app.py`, `README.md`, `PROJECTS.md`, `VERSION.txt`, `.gitignore`.

8. 테스트: `P1_102/tests` 16건 전량 통과, `P1/tests` 8건 회귀 없음, Xvfb 기반 보드 스모크
   테스트(4행 입력·depth4 무시·입력값 저장→재기동 복원·전체 탭 전환) 통과.
