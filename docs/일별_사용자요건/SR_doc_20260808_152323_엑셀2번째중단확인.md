* 최종 버전 --> 2.0.64
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 15:18 (KST) / 2026-08-08 06:18 (UTC)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 15:23 (KST)
3. 반영된 프로그램 버전: 2.0.64
4. 최종본 위치: GitHub (브랜치 `cursor/fix-row2-batch-stop-9027`), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

엑셀 2번째 자료에서 중단되는 문제 해결됐나 ..

6. 확인·조치 내용:

## (1) 확인 결과 — 원인 2건은 이미 main 반영됨

- v2.0.60 `4bab661`: 2번째 이후 입력행의 2단계 초기화를 1번째와 동일한
  실제 메뉴클릭(force=True)으로 통일 — 행마다 초기화 동작이 달랐던 문제.
- v2.0.62 `72b5844`: main() 행 루프의 `except RuntimeError(특정 문구만)`를
  `except Exception(전체)`로 교체 — 2번째 행 준비 중 예외 하나로 배치
  전체가 조용히 죽던 치명적 버그.

## (2) 회귀 테스트 신설 (재발 방지)

`P2/tests/test_row_loop_continues.py` — 브라우저 없이 main() 행 루프만 검증.

- 2번째 행 **준비 중** 예외 → 1·3번째 행은 계속 처리, 종료코드 2
- 2번째 행 **처리 중** 예외 → 3번째 행까지 진행
- 2번째 행 실패(False 반환) → 나머지 행 계속
- 사용자 '수집 종료'(CollectStopped)만 즉시 중단, 종료코드 130
- 전 행 성공 시 종료코드 0

검증: 수정 이전 코드(v2.0.61 `ef4232b`)에 이 테스트를 걸면 위 1·2번이
실제로 FAIL → 과거 버그를 정확히 재현하는 테스트임을 확인.

## (3) 남아 있던 "조용히 죽는" 구멍 차단

보드(`board/app.py::_handle_collect_line`)는 `##MAIN##/##SUB##/##META##`
마커 없는 줄을 화면에서 전부 버린다. 따라서 행 루프 **밖**(브라우저 연결,
로그인, 갤러리 기록 등)에서 예외가 나면 파이썬 트레이스백이 stderr로만
나가 화면에는 한 줄도 안 보인 채 종료됐다.

→ `collect.py::main()`에 최상위 예외 처리 추가: 예외 종류·메세지·트레이스백을
`ctx.info()`(##SUB## 마커)로 실행로그에 남기고 진행 상황(성공/실패/대상 행수)을
출력한 뒤 전용 종료코드 3으로 종료. 이제 어떤 경로로 죽든 화면에 이유가 남는다.
