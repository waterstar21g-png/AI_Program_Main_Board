* 최종 버전 --> 웹앱 2.4.4 / python-collector v1.4.0
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
웹앱 2.4.4 / python-collector v1.4.0

4. 최종본 위치
- 로컬PC: .\run.ps1 -Sync (웹앱) / collect.py 재다운로드 (python-collector)
- Vercel: Not-OK
- GitHub: main

5. 사용자 작성 원문 전체

```
[검색하신 검색에 대한 검색결과가 없습니다.]
[정확한 검색어인지 다시한번 확인하시고 검색해 주세요.

수동으로 하면 검색기록 모두 있는것으로 나타남..

조치 바람.
```

6. 반영 내용
- v1.2.0/v2.4.0에서 이미 "엉뚱한 필드 선택" 문제를 고쳤음에도 재발 보고
  → 원인을 정확히 좁히기 위해 "엑셀 원본 값"과 "실제 입력칸에 들어간
  최종 값"을 매 행마다 나란히 로그로 남기도록 강화 (웹앱 runner.ts,
  python-collector collect.py 동일하게 반영)
- normalizeUrl이 프로토콜(http/https)을 보정한 경우 그 사실도 별도로
  로그에 표시 (엑셀 값과 실제 검색값이 달라지는 지점을 명확히 드러냄)
- 입력값이 의도한 값과 다르면 [경고] 로그로 즉시 표시
- 다음 실패 시 사용자에게 "엑셀 원본 값" / "입력칸 최종 값" 두 줄과
  엑셀 해당 행의 실제 셀 값을 나란히 확인 요청 (원인을 추측이 아니라
  로그로 확정하기 위함)
- 타입체크·빌드·python 문법 검증 통과
