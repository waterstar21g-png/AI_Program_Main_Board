* 최종 버전 --> python-collector v1.1.0
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK (해당 없음)
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
python-collector v1.1.0

4. 최종본 위치
- 로컬PC: python-collector/run.bat (Chrome/Edge 기존 브라우저 사용)
- GitHub: main, /python-collector

5. 사용자 작성 원문 전체

```
로그인 건너뛰고 메인화면에서 시작해..
망고 url --> https://tmg1898.cafe24.com/mall/admin/admin.php
chromium 쓰지 말고 망고 기본 화면에서 처리해
파이쎤으로 구현시 화면처리 등에 기능적 제약이 있다면, php로 구현하는 것을 가능성을 검토 해봐 (참고 사항임)
```

6. 반영 내용
- MAIN_URL(admin.php) 추가. LOGIN_URL로 먼저 가지 않고 메인화면으로 바로 이동 → 세션 있으면 로그인 생략
- Playwright 전용 Chromium 다운로드/설치 전면 제거. 대신 PC의 기존 Chrome/Edge에 CDP(9222)로 연결
  - 이미 열려있는 망고 탭이 있으면 그 탭 그대로 재사용
  - 없으면 평소 쓰는 Chrome(없으면 Edge)을 디버그 모드로 새로 실행 후 연결
- run.bat에서 "playwright install chromium" 단계 삭제 (더 이상 필요 없음)
- 검증(Xvfb + 실제 Chromium으로 CDP 연결 재현):
  a) 새 브라우저 실행 → 메인화면 → 0.초기화 → 대량수집 화면 도달 확인
  b) 이미 열린 망고 탭 재사용 → 재이동 없이 바로 진행 확인
  c) 2행 전체 0→1→2→3→4 사이클 CDP 방식으로 재검증 통과
- PHP 전환 검토 결과: 기능적 제약 없음(파이썬 이슈는 이미 해결됨) → PHP 미채택, 근거를 사용자에게 설명
