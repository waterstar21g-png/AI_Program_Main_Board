* 최종 버전 --> 2.0.63
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 06:01 (UTC) / 2026-08-08 15:01 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 15:05 (KST)
3. 반영된 프로그램 버전: 2.0.63
4. 최종본 위치: GitHub main (병합 완료), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

(스크린샷: 보드 창 제목 "AI_Program_Main_Board v2.0.55", 좌측 사이드바
"v2.0.55 프로그램")

버전이 예날건데 이유가 뭐야?

6. 원인 규명:
- `board/app.py`에 `VERSION = "2.0.55"`가 **하드코딩**되어 있었고, 이는
  지금까지 계속 갱신해 온 저장소 루트 `VERSION.txt`와 완전히 별개의
  값이었음. 즉 실제 소스 코드(버그 수정들)는 git pull로 정상 갱신되어도
  보드 창 제목·사이드바에 찍히는 버전 숫자만 예전 값(2.0.55) 그대로
  남아있는 상태였음 — 이번 세션에서 v2.0.56~2.0.62까지 여러 번 버전을
  올렸지만 이 하드코딩 지점은 한 번도 같이 갱신되지 않았음(제 실수).

7. 조치 내용:
- `board/app.py`: 하드코딩된 `VERSION = "2.0.55"` 제거, 대신
  `_read_version()` 함수가 저장소 루트 `VERSION.txt`를 직접 읽어
  버전 숫자를 추출하도록 변경. 앞으로는 `VERSION.txt` 하나만 올리면
  보드 창 제목·사이드바 버전 표시도 항상 자동으로 일치함(다시는
  따로 놀지 않음).
