* 최종 버전 --> 2.0.53
* 로컬PC 보관 --> Not-OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> Not-OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-08 04:31 (UTC) / 2026-08-08 13:31 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-08 13:35 (KST)
3. 반영된 프로그램 버전: 2.0.53
4. 최종본 위치: GitHub (브랜치 `cursor/main-sub-timestamp-align-8612`), 로컬PC는 pull 후 적용, Vercel 해당 없음

5. 사용자 작성 원문 전체:

요구사항 은 채칭창에 입력하는 즉시, SR_DOC에 기록해..
(중간에 커서가 오류나면 전부 없어지면 안되니까)

다음 요구사항 반영. 

1.로그인 완료후 --> 2단계"초기화" --> 3단계 --> 4단계 진입까지 시간을 최소화해
  (2,3단계는 거의 불필요한 시간임 --> 컴튜터 작업속도로 처리해)

2. 실행로그 MAIN 상단에 5개 항목 추가 할 것
   현재 수행중인 엑셀 정보를 5개 항목으로 출력할것
   -> 필드명: '총건수', '완료건' , '순번', '수집 필드', '카테고리 URL' 
   
3. 작업을 엑셀 2개만 처리하고 중단하는 문제 해결해..
   --> 엑셀자료가 있는 동안 계속 수행해

4. 팝업창 대기시간을 제외하고....다음 작업에 대한 대기시간은 최소화 할것
  1) 로그인후 --> 2단계 '초기화'로 넘너가느 시간 
  2) 2단계 '초기화' 후 --> 다음 단계로 넘어가는 시간
  3) '데이터 필드값' 입력 시간
  4) '데이터 필드값' 입력후 -> '버튼'을 누르는 시간
  5) '데이터 필드값' 입력후 -> '버튼'을 누른후 --> 다음 단계로 넘어가는 시간
  6) 팝업창 닫기 확인후 -> 다음 단계로 넘어가는 시간 

5. 실행로그 MAIN과 SUB의 시간을 맞추기
  1) 기준 : SUB의 시간은 현단계 MAIN 의 진입시간 부터 다음단계 MAIN 의 진입시간 까지임.  OK?

당장 실행해 .....

6. 조치 내용:
- `P2/collect.py`: 로그인→2→3→4 구간 불필요 대기 제거(이미 대량수집 화면이면 메뉴 클릭 생략, URL검색버튼 보이면 즉시 반환, 로그인 후 500ms 제거).
- `##META##` 프로토콜·`emit_progress_meta()` — main 상단 5항목(총건수·완료건·순번·수집 필드·카테고리 URL) 실시간 갱신.
- `board/app.py`: 검증 모드에서 `--max-rows 2` 제거 → 엑셀 전체 행 연속 처리. META 5행 고정 표시.
- SUB 시각 = 현단계 MAIN 진입~다음 MAIN 진입 범위(`HH:MM:SS~HH:MM:SS`).
