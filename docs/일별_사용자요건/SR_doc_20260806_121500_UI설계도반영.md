* 최종 버전 --> 2.4.1
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간
2026-08-06 (KST)

2. 요구사항 반영 완료 날짜·시간
2026-08-06 (KST)

3. 반영된 프로그램 버전
2.4.1

4. 최종본 위치
- 로컬PC: .\run.ps1 -Sync 후 localhost:3000, 상품데이터수집 탭
- Vercel: Not-OK
- GitHub: main

5. 사용자 작성 원문 전체

```
"Failed to fetch"에 대해....이건 기존프로그램에서 난 오류를
화면캡처한 것으로...

지금 프로그램과 관련없음....무시.....

UI설계도만 참조하시기 바람
```
(직전 스크린샷: 좌측 프로그램목록 / 우측 1.엑셀업로드 · 2.실행(단계목록,
① 로그인→대량수집 자동, ② 수집 시작 두 버튼) · 실행 로그)

6. 반영 내용
- "Failed to fetch"는 무시, 스크린샷은 UI 레이아웃 참고용으로만 사용
- 화면 상단에 브레드크럼("상품데이터수집 → 수집용 엑셀 업로드 →
  대량수집 로그인 → 스텝별 자동 수집 반복") 추가
- 기존엔 실행 버튼이 "▶ 실행" 하나뿐이었음 → 설계도대로 2버튼으로 분리:
  - "① 로그인→대량수집 자동": /api/product-collect/open 호출,
    메인화면(admin.php) 진입·로그인 상태 확인 결과를 화면에 표시
  - "② 수집 시작": 기존 /api/product-collect/run 그대로(0~4 반복 실행)
- btn--secondary, panel__breadcrumb 스타일 추가
- 검증: 타입체크·프로덕션 빌드 통과, dev 서버 기동 확인(에러 없음)
