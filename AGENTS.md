# AGENTS.md — 이 저장소에서 작업하는 에이전트 규칙

이 파일이 **단일 진입점**이다. `docs/에이전트_안내.md` 는 요약·링크용이며, 상세 규칙은 여기와
`docs/일별_사용자요건/README.md` 를 따른다.

## 0. 작업 시작 전 (필수)

요구사항 원문이 **없으면** 구현·추측·대규모 탐색을 하지 않는다.
- 사용자 메시지 / SR 문서 / 연결된 이슈에 **구체 지시가 있는지** 먼저 확인한다.
- 원문이 없으면: 한두 문장으로 “할 일이 없다”고 밝히고, 원문 또는 대상(P1/P2/보드)을 요청한다.
- 런 제목·브랜치명만으로 기능을 만들어 넣지 않는다.

## 1. 작업 완료 보고 형식 (필수)

★요건(2026-08-20): 작업을 마치면 **항상 아래 5개 항목 폼으로** 간결하게 보고한다.
장황한 설명 대신 핵심만 담는다.

```
-변경 내용 (요약) ::
-기술적 제약(고려사항 및 향후 개선과제) ::
-변경 전후 버전 ::
-실행 방법 ::
-merge url ::
```

| 항목 | 담을 내용 |
|------|-----------|
| 변경 내용 (요약) | 무엇을 바꿨는지 항목별 한 줄씩 |
| 기술적 제약 | 남은 제약·수동 조치·미해결 이슈·향후 개선 방향 |
| 변경 전후 버전 | `VERSION.txt` 기준 `vX.Y.Z → vX.Y.Z` (문서만이면 동일 버전 명시) |
| 실행 방법 | 사용자가 실제로 눌러야 하는 순서 (버전갱신 포함) |
| merge url | 해당 PR 주소 |

## 2. 머지 URL · 로컬 반영 (필수)

PR이 열렸거나 머지가 필요하면 응답에 **머지 URL을 항상** 넣는다.
사용자가 묻기 전에 먼저 표시하고, PR 본문 상단에도 `## 머지 URL` 로 동일 링크를 둔다.

로컬 반영 방법(고정):

1. 응답에 **머지 URL** 표시 (필요 시 사용자가 GitHub에서 머지)
2. 바탕화면 **「AI_보드_버전갱신」** 아이콘 한 번  
   → 보드 종료 → GitHub `main` 강제 반영 → 보드 재시작  
   (또는 보드 좌측 하단「머지반영 업데이트」— 종료 후 외부 강제갱신)

**금지:** `run.bat` / `run.bat --noupdate` 를 머지 반영용으로 **따로 실행하라고 안내하지 않는다.**  
아이콘이 없으면(최초 1회) `아이콘새로만들기.bat` 만 안내한다.

## 3. 사용자 요건 원문 보관 (필수)

커밋마다 `docs/일별_사용자요건/SR_doc_YYYYMMDD_HHMMSS_요건요약20자이내.md` 를
새로 만들어 **사용자 원문을 편집 없이** 보관한다. 상세 규칙은
[`docs/일별_사용자요건/README.md`](./docs/일별_사용자요건/README.md) 참조.

원문이 없으면 추측하지 말고, 이번 메시지 원문만 보관한다.

## 4. 버전 규칙

- 단일 소스는 루트 `VERSION.txt` — 보드 제목·사이드바가 이 파일을 직접 읽는다
  (`board/app.py` 의 `_read_version()`).
- 코드를 바꿨으면 `VERSION.txt` 와 `README.md` 첫 줄의 버전을 함께 올린다.
- **문서·규칙만** 바꾼 커밋은 버전을 올리지 않아도 된다 (보고에 동일 버전을 적는다).
- 롤백을 하더라도 **버전 번호는 되돌리지 않고 전진**시킨다 —
  `update-if-newer.ps1` 이 버전 문자열 비교로 갱신을 판단하므로, 서로 다른 코드가
  같은 번호를 가지면 안 된다.

## 5. 프로그램 범위

현재 보드 프로그램은 `PROJECTS.md` / `README.md` 표를 따른다.
- P1, P1_102, P1_101, P1_ZARA_DE, P2(구 P3), P3_필터_갱신
- UI: `board/app.py` (Python). `legacy-next/` 는 보관용.
