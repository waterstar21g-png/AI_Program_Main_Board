# AGENTS.md — 이 저장소에서 작업하는 에이전트 규칙

## 작업 완료 보고 형식 (필수)

★요건(2026-08-20): 작업을 마치면 **항상 아래 5개 항목 폼으로** 간결하게 보고한다.
장황한 설명 대신 핵심만 담는다.

```
-변경 내용 (요약) ::
-기술적 제약(고려사항 및 향후 개선과제) ::
-변경 전후 버전 ::
-실행 방법 ::
-merge url ::
```

| 항목 | 담을 내용 |
|------|-----------|
| 변경 내용 (요약) | 무엇을 바꿨는지 항목별 한 줄씩 |
| 기술적 제약 | 남은 제약·수동 조치·미해결 이슈·향후 개선 방향 |
| 변경 전후 버전 | `VERSION.txt` 기준 `vX.Y.Z → vX.Y.Z` |
| 실행 방법 | 사용자가 실제로 눌러야 하는 순서 (버전갱신 포함) |
| merge url | 해당 PR 주소 |

## 사용자 요건 원문 보관 (필수)

커밋마다 `docs/일별_사용자요건/SR_doc_YYYYMMDD_HHMMSS_요건요약20자이내.md` 를
새로 만들어 **사용자 원문을 편집 없이** 보관한다. 상세 규칙은
[`docs/일별_사용자요건/README.md`](./docs/일별_사용자요건/README.md) 참조.

## 버전 규칙

- 단일 소스는 루트 `VERSION.txt` — 보드 제목·사이드바가 이 파일을 직접 읽는다
  (`board/app.py` 의 `_read_version()`).
- 코드를 바꿨으면 `VERSION.txt` 와 `README.md` 첫 줄의 버전을 함께 올린다.
- 롤백을 하더라도 **버전 번호는 되돌리지 않고 전진**시킨다 —
  `update-if-newer.ps1` 이 버전 문자열 비교로 갱신을 판단하므로, 서로 다른 코드가
  같은 번호를 가지면 안 된다.
