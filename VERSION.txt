버전 2.1.55 (Python B안)
업데이트: Cursor Cloud Agent 개발환경 구성.
1) `.cursor/environment.json` + `.cursor/install.sh` 신규 —
   헤드리스 리눅스에서 Tkinter 보드(board/app.py)와 Playwright 크롤러가
   바로 실행되도록 시스템 의존성(python3-tk·Xvfb·CJK 폰트·imagemagick·xdotool)과
   Python 의존성(requirements.txt + pytest), Playwright(Chromium)을 자동 설치.
2) `terminals` 로 Xvfb(:99) 가상 디스플레이 상시 기동 —
   `DISPLAY=:99 python3 board/app.py` 로 GUI 를 헤드리스로 띄울 수 있음.
3) 로컬 개발용 `.venv/` 는 .gitignore 에 추가.
코드 로직 변경 없음 — 개발환경 설정만 추가.
